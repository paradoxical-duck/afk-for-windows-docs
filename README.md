# AFK for Windows Docs

Firebase Hosting docs for AFK for Windows.

The Windows installer is deployed from `public/downloads/AFK-Setup-0.1.0.zip` but is intentionally ignored by Git because it is larger than GitHub's standard file limit.

## Commands

```powershell
npx firebase-tools deploy --only hosting
```

